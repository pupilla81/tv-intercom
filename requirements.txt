# TV Intercom — Dipendenze Python

# --- Modulo 1: Script Parser + Cue Engine ---
rapidfuzz>=3.6.0          # fuzzy string matching per il tracking del copione

# --- Modulo 2: STT Tracker (da installare quando si sviluppa il modulo) ---
# openai-whisper>=20231117  # STT locale
# sounddevice>=0.4.6        # acquisizione audio dalla scheda audio
# numpy>=1.24.0

# --- Modulo 3: Dispatcher (da installare quando si sviluppa il modulo) ---
# livekit>=0.11.0           # WebRTC per l'invio audio agli operatori

# --- Modulo 4: Server (da installare quando si sviluppa il modulo) ---
# fastapi>=0.109.0
# uvicorn>=0.27.0
# websockets>=12.0
